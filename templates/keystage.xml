<keystages>
    [%- FOREACH keystage IN keystages %]
    <keystage
        [%- FOREACH key IN keystage.keys() %]
            [%- IF keystage.$key.length %]
                [% key | html %]="[% keystage.$key | html %]"
            [%- END %]
        [%- END %]
    />
    [%- END %]
</keystages>
