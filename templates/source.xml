<sources>
    [%- FOREACH source IN sources %]
    <source
        [%- FOREACH key IN source.keys() %]
            [%- IF source.$key.length %]
                [% key | html %]="[% source.$key | html %]"
            [%- END %]
        [%- END %]
    />
    [%- END %]
</sources>
